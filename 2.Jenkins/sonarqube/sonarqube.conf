# REverse proxy configuration for matching the subdomain to the service running in container

server {
    listen 80;
    server_name sonarqube-sc.anajak-khmer.site;

    location / {
        proxy_pass http://localhost:3005;

        # Preserve original host and client info
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;


        # WebSocket support (VERY IMPORTANT)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        # Prevent caching issues
        # proxy_cache_bypass $http_upgrade;
    }
}
# Jenkins variables 
There are two type of variables inside the JENKINs 
1. Built-in : Can check it by `JENKIN-URL/env-vars.html` 
ex. ${env.BUILD_NUMBER}
2. User-defined: type of variables that you created yourself


# 2. Working with Credentials 